# gitgrep
Search across multiple git repositories concurrently.
```bash
python gitgrep.py "TODO" ~/projects
python gitgrep.py "import os" ~/projects -f "*.py" -l
```
## Zero dependencies. Python 3.6+.
